# Prompt compiler API

The compiler converts one canonical task profile into the native prompt format
expected by every registered method.

::: common.prompts.compiler
    options:
      members:
        - load_prompt_profile
        - compile_task_prompt_assets
