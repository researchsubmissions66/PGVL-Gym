# PGVL-Gym pip-only compatibility install.
#
# The recommended GPU setup is:
#   conda env create --file environment.yml
#   conda activate pgvl-gym
#
# This file delegates to pyproject.toml so the dependency definitions cannot
# drift apart. It does not choose a CUDA runtime; install PyTorch explicitly
# first when not using environment.yml.
-e .[all]

# Gated upstream packages remain explicit:
#   pip install "git+https://github.com/mahmoodlab/CONCH.git"
#   pip install fairscale "git+https://github.com/lilab-stanford/MUSK.git"
