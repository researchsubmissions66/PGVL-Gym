backbone: keep
backbone_weights: ${PGVL_STORAGE_ROOT}/.cache_huggingface/hub/models--Astaxanthin--KEEP/snapshots/28a25d95cc6ba27a7e6fab3f144e13dbafd8b21e
encoder:
  name: keep
  weights: ${PGVL_STORAGE_ROOT}/.cache_huggingface/hub/models--Astaxanthin--KEEP/snapshots/28a25d95cc6ba27a7e6fab3f144e13dbafd8b21e
  feature_space_id: hf:Astaxanthin/KEEP
  feature_dim: 768
  initialization: pretrained
feature_space_id: hf:Astaxanthin/KEEP
n_classes: 2
classnames:
- invasive ductal carcinoma
- invasive lobular carcinoma
label_dict:
  IDC: 0
  ILC: 1
shots: 16
k: 5
k_start: 0
k_end: 5
seed: 1
batch_size: 1
num_workers: 4
feature_key: features
feature_sources:
  bag: keep_20x
feature_resolutions:
  bag: 20x
feature_input_kinds:
  bag: patch_bag
include_metadata: true
dataset_csv: ${PGVL_REPO_ROOT}/benchmarks/tcga_brca/data/brca/manifest.csv
split_dir: ${PGVL_REPO_ROOT}/benchmarks/tcga_brca/splits/brca/16shot
task: brca
benchmark: tcga_brca_resolution_registry
feature_dim: 768
method: pathpt
experiment: pathpt_keep
epochs: 20
training_mode: upstream_patch_ssl
prompt_provenance: upstream
implementation_provenance: vendored_native_objective
upstream_fidelity: upstream
fidelity_note: Native PathPT patch-supervision pipeline; prompt origin is recorded separately.
results_dir: ${PGVL_REPO_ROOT}/results/tcga_benchmark/pathpt_keep/brca/16shot
lr: 0.0001
n_ctx: 32
prompt_init: template
aux_weight: 0.5
learnable: token
vision_only: false
vision_grad: true
use_aug: false
loss_weight:
- 1.0
- 0.5
- 0.1
balance: true
enable_pseudo: true
prompt_select: true
logits_thd: 0.0
prompt_classifier_count: 200
prompt_select_count: 100
prompt_top_patches: 100
eval_patch_batch_size: 50000
early_stopping: false
feature_root: ${PGVL_STORAGE_ROOT}/trident_features/master_benchmark/20x_256px_0px_overlap/features_keep
feature_path_column: feature__keep_20x
patch_num: null
loader_mode: h5
prompt_source: pathpt_upstream_22_template_bank
pathpt_prompt_bank_source: MAGIC-AI4Med/PathPT utils.py:brca_names
pathpt_synthetic_normal: true
encoder_provenance: native
